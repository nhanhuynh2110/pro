datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:@@2018b13b2813@db.fcbnymwabtoqyvaotuje.supabase.co:5432/postgres?schema=public"
}

model User {
  id    Int    @id @default(autoincrement())
  name  String
  posts Post[]
}

model Post {
  id        Int     @id @default(autoincrement())
  title     String
  published Boolean @default(true)
  authorId  Int
  author    User    @relation(fields: [authorId], references: [id])
}